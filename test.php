<?php
require 'somefile.php';
$var = 'Roberto';
$Var = 'Juan';
$_4site = 'aun no';     // válido; comienza con un carácter de subrayado
# Bryan Steven Cardona
# Andres Manuel Prieto
# Juan Esteban Velasquez

if ($left > $right)
    {
        return 1;
    }

echo "ger SDASJAKLX AXANKJXA";


# Este es un comentario de una linea
function RecursiveBinarySearch()
// Este es otro comentario de una linea
{
    echo "ger SDASJAKLX AXANKJXA";
    if ($left > $right)
    {
        return 1;
    }
    /* Este es un comentario de
    varias lineas*/

    if ($comparision_result == 0)
    {
        return $average_element_index;
    }
    else
    {
        if ($comparision_result > 0)
        {
            $left = $average_element_index;
            return RecursiveBinarySearch($array, $search, $left, $right);
        }
        else
        {
            $right = $average_element_index;
            return RecursiveBinarySearch($array, $search, $left, $right);
        }
    }
}

?>